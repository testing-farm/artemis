---

# a default section is not used when there is a configuration

section:
  - a command
  - another command

kwargs:
  default_commands:
    - a default command

expected:
  default:
    - a command
    - another command
